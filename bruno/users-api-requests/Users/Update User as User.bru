meta {
  name: Update User as User
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/users/6
  body: json
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

body:json {
  {
    "name": "João Silva Atualizado"
  }
}
